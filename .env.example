# Application ports
API_PORT=3000
WEB_PORT=5173

# Local PostgreSQL development credentials
POSTGRES_DB=restgest_mateos
POSTGRES_USER=restgest
POSTGRES_PASSWORD=change-me-for-local-development
POSTGRES_PORT=5433

# Keep this value consistent with the PostgreSQL variables above.
DATABASE_URL=postgresql://restgest:change-me-for-local-development@localhost:5433/restgest_mateos?schema=public
